# 📌 Debezium CDC + Kafka + PostgreSQL + Prometheus + Grafana

Полный цикл **Change Data Capture (CDC)**, реализованный с помощью **Debezium PostgreSQL Connector**.

Этот проект демонстрирует, как Debezium отслеживает изменения в таблицах `users` и `orders` в PostgreSQL и публикует их в Kafka. Для мониторинга Kafka Connect используются **Prometheus** и **Grafana**.

Все сервисы запускаются и оркестрируются с помощью **Docker Compose**.

---

## 🧱 Архитектура

**Поток данных:**
`PostgreSQL` → `Debezium` → `Kafka` → `Python Consumer` → `Kafka Connect` → `Prometheus` → `Grafana`

**Компоненты проекта:**

- **PostgreSQL** — источник данных (Source DB).
- **Debezium Connector** — плагин для CDC слежения.
- **Kafka + Zookeeper** — брокер сообщений и координатор.
- **Kafka Connect** — среда выполнения для запуска коннектора.
- **Prometheus** — сбор метрик (JMX exporter).
- **Grafana** — визуализация метрик.
- **Python Consumer** — скрипт для чтения и обработки событий из Kafka.

---

## 🚀 Быстрый запуск

### 1. Клонирование проекта

```bash
git clone https://github.com/<your_github>/debezium-cdc.git
cd debezium-cdc